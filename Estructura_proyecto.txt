Estructura del proyecto

											Interfaces base
	Interfaz ICarta
Atributos:
Color : string
Valor : string
Tipo : string
Métodos:
MostrarCarta()
GetValorNumerico() Int

Interfaz IJugador
Métodos:
RecibirCarta(ICarta carta)
MostrarMano()
TomarDecision(Ijuego juegoContexto)
ObtenerPuntos()
GetNombre()
IReadOnlyList<ICarta>
ReportarAccion(string mensaje)
GetId()

Propiedades:
Estrategia : IEstrategiaJugador

Interfaz IEstrategiaJugador
Metodos:
EjecutarDecision(IJugador jugador, IJuego juego)


Interfaz IJuego
Métodos:
IniciarJuego()
RepartirCartas()
JugarTurno()
MostrarEstado()
DeterminarGanador()
FinalizarJuego()

Interfaz IEstadoJuego
Atributos:
NombreJuego : string
Jugadores : IReadOnlyList<IJugador>
Mazo : IReadOnlyList<ICarta>
Descarte : IReadOnlyList<ICarta>
TurnoActual : int


  												CLASES BASES

  	Clase Carta : ICarta (abstracta)
Atributos:
Color : string (protected init)
Valor : string (protected init)
Tipo  : string (protected init)

Constructor:
Carta(color : string, valor : string, tipo : string)

Métodos:
MostrarCarta()  (imprime la representación textual de la carta en consola)
Virtual GetValorNumerico() = int (definido en subclases según el tipo de carta)
Override ToString() (devuelve una descripción legible de la carta) (“Valor de Color (Tipo)”)

    Clase Jugador : IJugador
Atributos:
nombre : string
idJugador : int
mano : List<Carta>
puntos : int
estrategia: IEstrategiaJugador

Métodos:
RecibirCarta(Carta c)
MostrarMano()
TomarDecision() 
abstract obtenerPuntos()
GetNombre()

    Clase Juego: IJuego
Atributos:
nombreJuego : string
jugadores : List<Jugador>
mazo : List<Carta>
descarte : List<Carta>
turnoActual : int
Métodos:
IniciarJuego()
RepartirCartas()
Virtual JugarTurno()
MostrarEstado()
DeterminarGanador()

			Comportamiento de los jugadores (Estrategia)
Interfaz IEstrategiaJugador
Metodos:
TomarDecision(Jugador jugador, Juego juego)

	Clase EstrategiaAleatoria : IEstrategiaJugador
Metodos:
TomarDecision(Jugador jugador, Juego juego)

	Clase EstrategiaCalculadora : IEstrategiaJugador
Metodos:
TomarDecision(Jugador jugador, Juego juego)

	Clase EstrategiaCautelosa : IEstrategiaJugador
Atributos:
limitePuntos : int

Metodos:
TomarDecision(Jugador jugador, Juego juego)

	Clase EstrategiaTemeraria : IEstrategiaJugador
Metodos:
TomarDecision(Jugador jugador, Juego juego)

		Creacion de jugadores
Clase CreadorDeJugadores
Metodos:
CrearJugador(tipo : string, nombre : string, estrategia : IEstrategiaJugador) -> IJugador

						UNO 
	Clase CartaUNO
Atributos:
tipoEspecial:TipoEspecialUNO (llama al Enum para identificar el tipo de carta especial en uso)
esEspecial : bool
efecto : string (para cartas como +2, +4, reversa, bloqueo, cambioColor)

Métodos:
AplicarEfecto(JuegoUNO juego, JugadorUNO siguiente)
EsComodinVerdadero():bool (da true si la carta es +4 o Comodin)
	
Enum TipoEspecialUNO
Valores:
MasDos
Reversa
Bloqueo
CambioColor
MasCuatro	

	Clase JugadorUNO : Jugador

Métodos:
ObtenerCartasValidas()
JugarCartaValida()
override ObtenerPuntos()


	Clase JuegoUNO:Juego
Atributos:
sentidoHorario : bool
Métodos:
override IniciarJuego()
override JugarTurno()
AplicarEfectoCarta()
override DetermianarJugadorGanador()
override MostrarEstado()


			BLACKJACK

Clase CartaPoker: Carta
Atributos:
figura : string (espadas, corazones, diamantes o tréboles)

Métodos:
override GetValorNumerico() → devuelve puntaje (2–10, J/Q/K = 10, As = 1 u 11)
Clase JugadorBlackjack:Jugador
Métodos:
CalcularPuntaje()
override ObtenerPuntos()

Clase Dealer: JugadorBlackjack

Métodos:
BarajarMazo()
RepartirCartas()
override TomarDecision() → saca cartas hasta tener 17 o más puntos

Clase JuegoBlackjack: Juego
Atributos:
dealer : Dealer
rondas : int
Métodos:
override IniciarJuego()
JugarRonda()
CalcularPuntos(Jugador J)
CompararConDealer()
override DeterminarGanador()
override MOstrarEstado()


		CLASES DE APOYO
Class Logger
Metodo: static Registrar(string mensaje) (para imprimir o guardar la accion en archivo

Clase simulador
metodos: Ejecutar() (aca se preguntara que juego se va acorrer, creara los jugadores usando CreadorDeJugadores y llama IniciarJuego y muestra los logs)
